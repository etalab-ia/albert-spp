version: "3.8"

services:
  redis:
    image: redis:7.2.4
    restart: always
    healthcheck:
        test: [ "CMD", "redis-cli", "--raw", "incr", "ping" ]
        interval: 4s
        timeout: 10s
        retries: 5
    volumes:
      - redis:/data

  api:
    image: albert-spp/api:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    restart: always
    command: uvicorn api.app:app --proxy-headers --forwarded-allow-ips='*' --host 0.0.0.0 --port 8000 --log-level debug
    environment:
      - API_KEYS=changeme
      - ENV=dev
      - APP_VERSION=1.0.0
      - MODEL_NAME=AgentPublic/llama3-fabrique-texte
      - MODELS_URLS=["https://localhost:8001"]
    depends_on:
      redis:
        condition: service_healthy

volumes:
  redis: